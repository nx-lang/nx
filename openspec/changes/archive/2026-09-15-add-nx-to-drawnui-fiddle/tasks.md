## 1. NX repository: compact IR and the prelude build helper

- [x] 1.1 Add a formatting choice to `emit_nx_ir` in `crates/nx-codegen` (compact or pretty), make `nx-ffi` and `bindings/wasm/native` emit compact JSON and keep `nx-cli`'s `nx-ir` target pretty-printed; verify `cargo test --workspace` passes, a wasm-emitted IR contains no newline, and the CLI's written file still does
- [x] 1.2 Update every test that compares emitted IR as text (the codegen tests, `runtime/typescript/test/emitted-ir.test.mjs`, the wasm and Node parity tests, the .NET binding tests) to compare parsed values; verify `pnpm -r test` and `dotnet test bindings/dotnet/NxLang.sln` pass
- [x] 1.3 Add `buildProgramWithPrelude(host, prelude, source, { fileName })` to `@nx-lang/sdk-wasm`, moving the origin classification and span shifting from `sites/playground/src/compile/catalog.ts` and using `@nx-lang/language-core`'s prelude offsets; verify unit tests cover a document error reported in document coordinates, a broken prelude reported as `catalog` with no IR, a clean build with IR, and the zero-width insertion-point case
- [x] 1.4 Turn `sites/playground/src/compile/catalog.ts` into a re-export over the SDK helper and delete the moved code and its duplicated tests; verify `pnpm run typecheck`, `pnpm test` and `pnpm run check-examples` in `sites/playground` pass
- [x] 1.5 Document the helper and compact output in `bindings/wasm/README.md`; verify the README describes the three diagnostic origins and the shifted spans

## 2. NX repository: packages a host can install

- [x] 2.1 Remove `private` from `@nx-lang/sdk-wasm` and `@nx-lang/ir-runtime`, add `publishConfig.access: public`, `files`, `repository` and README entries matching `@nx-lang/monaco`; verify `pnpm pack` of each produces a tarball whose listing includes `dist/nx.wasm` for the SDK and `.d.ts` files for both
- [x] 2.2 Make `@nx-lang/monaco`'s `monaco-editor` imports type-only, lower its peer range to `>= 0.52.0`, and state in its README that an AMD-loaded global namespace is accepted; verify the built package has no runtime import of `monaco-editor` and a test registers against a 0.52-shaped namespace stub with hover, completion and markers exercised
- [x] 2.3 Extend `scripts/verify-package.mjs` (or its equivalent) so the SDK and runtime packages pass the workspace's package verification; verify `pnpm run verify:packages` is green
- [x] 2.4 Extend the package release track (`build.yml`, `release.yml`, `package-publish.yml`) to pack each publishable workspace package after `pnpm -r build`, verify workspace references were rewritten to the tag version, attach every `.tgz` to the draft release, and publish them in dependency order with duplicate-version skipping; verify `actionlint` passes and a pull request run uploads one `.tgz` per package
- [x] 2.5 Update `docs/deployment-setup.md` and `docs/deployment.md` for the additional npm packages and their repair procedure; verify the runbook lists every package the publish job pushes
- [x] 2.6 Cut a release tag and publish it; verify `npm view @nx-lang/sdk-wasm version`, `npm view @nx-lang/ir-runtime version` and `npm view @nx-lang/monaco version` answer with the tag's version and a scratch project can install and import all three
- [x] 2.7 Once 2.6 has published them, move the four `@nx-lang/*` pins in the fiddle's `package.json` from `optionalDependencies` to `devDependencies` and re-run `npm install` so `package-lock.json` records resolved versions rather than the unresolvable `{"optional": true}` stubs it holds while they are unpublished; verify `npm ci` installs them and `npm run runtime` builds without `--nx`

## 3. Fiddle repository: catalog generated from the pinned DrawnUi.React

- [x] 3.1 Add `typescript` and the `@nx-lang/*`, `shiki` and `@shikijs/monaco` devDependencies to `package.json`, and an `--nx <checkout>` option to the runtime build that resolves `@nx-lang/*` from a sibling NX workspace; verify `npm install` succeeds and the option is documented in `AGENTS.md`
- [x] 3.2 Write `nx/generate-catalog.mjs`: enumerate React tags from `drawnui-react/dist/react/index.d.ts`, resolve each tag's props through the TypeScript checker, and emit `nx/catalog/drawnui.nx` and `nx/catalog/catalog-meta.json` (unions, records with constructing class, components with content property, node root, and the `drawnui-react` version); verify two runs produce identical output and every exported tag has a component
- [x] 3.3 Diff the generated catalog against the playground's for the tags both DrawnUi.React versions share and record intended divergences in `nx/CATALOG.md`; verify the notes explain every difference that is not a preview.9 addition
- [x] 3.4 Add `npm run catalog` and a stale-catalog check in the runtime build that compares the catalog's recorded version to the pin; verify a deliberately mismatched version fails the build naming both versions and the command

## 4. Fiddle repository: the NX runtime bundle

- [x] 4.1 Write `nx/src/values.ts` and `nx/src/draw.tsx`, ported from the playground's coercion and walk, taking `React` and the DrawnUI namespace from the window globals and constructing `Thickness`, `CornerRadius`, `SkiaPoint`, `SkiaShadow` and `SkiaBevel` through them; verify a Node test with stub globals coerces a union case to its bare name, constructs a record through the supplied class, drops nulls, and emits a placeholder for an unknown type
- [x] 4.2 Write `nx/src/runtime.ts`: load the module once from `<root>/nx/nx.wasm`, create the host, expose `compile(code)` through `buildProgramWithPrelude` over the catalog (returning `{ js, errors, warnings }` with `L{line}: message` errors from `source` diagnostics and readable messages for `catalog` and `program` faults), `diagnostics(code)` over the SDK's snapshot language service, `configure(monaco)` through `registerNxLanguage` with the `editor.nx` workspace, and `mount(ir)` as a memoized component that prepares, evaluates `root` and draws; verify a Node test compiles a preset to a module whose text embeds compact IR and reports a bad snippet with its line
- [x] 4.3 Add trap recovery: on a crashed host, rebuild it from the compiled module before the next compile; verify a test with the SDK's debug-trap module sees one failed compile followed by a successful one
- [x] 4.4 Write `dev/build-nx-runtime.mjs` (esbuild IIFE setting `window.NxDrawn`, React and DrawnUI as externals, the catalog inlined, Shiki bundled) writing `nx-runtime.js` and copying `nx.wasm` into `<host>/wwwroot/nx/`, and run it from `npm run runtime` with the same `--out` handling as the React runtime; verify the dev host's `wwwroot/nx/` holds both files and `--out` writes elsewhere
- [x] 4.5 Add a preset compile test to the runtime build that compiles every NX preset against the catalog; verify it fails when a preset uses an undeclared property

## 5. Fiddle repository: engine hooks and the NX language

- [x] 5.1 Add the `prepare` hook to `fiddle-react.js`: awaited before `compile` in `fiddleReactRun`, and before evaluating the stored module in `fiddleReactPlay` for the language named by `X-Fiddle-Lang`, with a rejection surfaced as a run failure and retried next run; verify TSX runs and plays unchanged and a registration whose `prepare` rejects shows the message under the editor
- [x] 5.2 Replace the hardcoded color-pattern table in `fiddle-intellisense.js` with patterns read from language registrations, keeping the C# and TypeScript patterns as the fallback; verify swatches still appear for C# and TSX color literals
- [x] 5.3 Write `src/DrawnUi.Fiddle/wwwroot/fiddle-nx.js`: register `nx` with `prepare` (load `nx/nx-runtime.js` once, then the module), `configure`, `compile`, `colors`, and expose `window.fiddleNxDiagnostics`; load it from `Fiddle.DevHost/wwwroot/index.html` at the marked spot; verify the editor page makes no request for `nx-runtime.js` or `nx.wasm` until NX is run
- [x] 5.4 Add `NxLanguage : ReactSurfaceLanguage` (`Id = "nx"`, `MonacoLanguage = "nx"`, display name, editor lead, `IsForeignCode` true for non-empty text without a `root` declaration, `DiagnosticsAsync` over `fiddleNxDiagnostics`) and register it after TSX in `ServiceCollectionExtensions.cs`; verify the language switch shows NX third and `fiddle.setCode(code, 'nx')` followed by `fiddle.run()` reports success
- [x] 5.5 Add `FiddlePresetsNx.cs` with at least four presets ported from playground examples (hello starter first, fonts and absolute-URL images only) and `FiddlePresets.LangNx`; verify each preset draws in the dev host and `fiddle.listPresets()` lists them with `lang` `nx`

## 6. Verification and documentation

- [x] 6.1 Run the `docs/ADDING-A-LANGUAGE.md` checklist in the dev host with the local backend: starter draws, a broken snippet shows `L{line}` errors and squiggles, HotReload waits for a clean snippet, hover and completion answer over catalog components, color swatches show, switching among C#, React and NX redraws each surface, gestures mode reaches the canvas; verify each step by hand and record the result in the pull request
- [x] 6.2 Share an NX snippet and open `/p/{id}`; verify the player draws it, posts `ready`, produces a thumbnail, and the network log shows the runtime bundle but no `nx.wasm`; open the share on a host with NX unregistered and verify the source is shown with the unknown-language notice
- [x] 6.3 Measure the share artifact size for each preset and record the numbers in the fiddle README's NX section; verify the largest is within the private backend's limit as confirmed with its owner, or record the follow-up if not
- [x] 6.4 Update `docs/ADDING-A-LANGUAGE.md` (the `prepare` hook, `colors`, NX as the third reference language), `AGENTS.md` (the `nx/` area, `npm run catalog`, the `--nx` option), and `README.md` (NX among the languages); verify the docs match the tree
- [x] 6.5 In the NX repository, add the fiddle to `specs/future.md`'s follow-ups: removing DrawnUI from the playground, the catalog as a library artifact for shares, and the fiddle compile worker; verify the section names each with its blocker
