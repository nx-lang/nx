## 1. HIR: derived update records

- [x] 1.1 Add `RecordKind::Update { target: Name }` in `crates/nx-hir/src/lib.rs` and thread it through every `match` on `RecordKind` (`records.rs` kind labels, `lib.rs` helpers). Verify `cargo build -p nx-hir` is clean and the existing `is_action` helper still returns false for update records.
- [x] 1.2 In `crates/nx-hir/src/lower.rs` predeclaration, synthesize `<Name>.Update` for every `type` record, `action`, inline emit record, and component with state (effective state fields only), with defaults stripped and every field optional, and register it beside `predeclared_action_records`. Verify an inline lowering test shows `User.Update` and `Counter.Update` as `Item::Record` with `RecordKind::Update` and the expected fields, and that a component's update record excludes props.
- [x] 1.3 Compute update-record fields through the base-chain resolver so inherited fields are included. Verify a lowering test for `abstract type Named = { name:string } type User extends Named = { email:string }` shows both fields on `User.Update`.
- [x] 1.4 Reject `extends` targeting an update record and reject an inline emit or emit reference named `Update` with the reservation diagnostic; do not synthesize `X.Update.Update`, so the checker reports it as an unknown update record. Verify lowering tests report the two reservation diagnostics and show the nested record absent (design D1, component-syntax spec).
- [x] 1.5 Add `owner: Option<Name>` to `ast::Expr::ActionHandler`, fill it during lowering from the enclosing component, and carry it through `promote_component_handler_bindings` and the `collect_handler_rewrites_*` walkers. Verify a lowering test shows `owner == Some("Counter")` for a handler bound inside `Counter` and `None` at root.

## 2. Type checking

- [x] 2.1 Give the checker a `current_component` set for the span of `infer_component` (prop defaults, state defaults, body). Verify by a unit test on `check.rs` internals or an observable diagnostic in 2.3. *(Not needed: the bare tag is resolved during lowering (design D3) and a handler's owner travels on `Expr::ActionHandler`, so the checker keeps no component state.)*
- [x] 2.2 Add the update-record construction rules in `crates/nx-types/src/infer.rs`: no required fields, no defaults, unknown field rejected, `null` only for nullable target fields, field types copied from the target. Verify tests in a new `crates/nx-types/tests/update_records.rs` cover the six scenarios of the update-records "absent means unchanged" requirement.
- [x] 2.3 Resolve the bare `Update` tag during lowering (design D3): inside a component rewrite it to `<Component>.Update` before the element-versus-record-literal decision; outside, leave it as written so the checker reports the diagnostic naming the `<Type>.Update` form. Verify tests for handler-body, state-default, root-level, and same-named-declaration scenarios, and that the lowered module carries the qualified name.
- [x] 2.4 Replace the `Type::Error` arm for `ActionHandler` with inference: bind `action` to the emit's action type, infer the body in the enclosing scope, stop skipping handler props in `check_property_path_bindings`. *(The body is checked where the `ActionHandler` expression is inferred; a handler prop still has no declared type to check against.)* Verify tests for the five "handler bodies are type checked" scenarios, including the loop-variable case.
- [x] 2.5 Implement result routing at the binding site (design D4): owner's update record, owner's effective emits, or anything at root; empty list rejected. Verify tests for all six routing scenarios, including the "suggest adding to emits" diagnostic text.
- [x] 2.6 Run `cargo test -p nx-types -p nx-hir` and fix any existing fixture that fails because handler bodies are now checked or because of the routing rule. Verify the full crate suites pass and note each fixture changed in the task's commit message. *(No nx-types fixture changed; two nx-hir tests that indexed items by position were updated in 1.2.)*

## 3. Interpreter: construction, live state, dispatch

- [x] 3.1 Add the update-record arm to `build_record_value_from_shape` (and `construct_external_record_value`) in `crates/nx-interpreter/src/interpreter.rs`: absent stays absent, `null` only for nullable, unknown field rejected. Verify inline tests construct `<User.Update email={null} />` to a record containing only `email`, and that unknown-field and non-nullable-null inputs fail with errors naming the field.
- [x] 3.2 Carry `owner` and `owner_state` on `Value::ActionHandler`, `SerializedValue::ActionHandler`, and `eval_action_handler_expr`; bump the snapshot `version`. Verify snapshot round-trip tests still pass and a pre-bump snapshot is rejected with the revision diagnostic.
- [x] 3.3 Extend `normalize_handler_result` / `ensure_action_result_value` to accept `RecordKind::Update` records. Verify a test where a handler returns `[<Update count={count + 1} />, <Saved />]` yields a two-item list, and that `[]`, a string, and a plain record are still rejected.
- [x] 3.4 Add the handler-token walk: assign tokens to handlers in a rendered value, store serialized handlers in a new snapshot `handlers` table, and rewrite the output value to `{ $type: "ActionHandler", action, token }`; pure evaluate emits the same record without a token. *(The token is carried on the rendered `Value::ActionHandler` and written into the record by `to_nx_value` (3.7), so Rust callers keep invocable handler values.)* Verify tests on `initialize_component` output (token present) and `evaluate_component` output (no token).
- [x] 3.5 Accept `{ $type: "ActionHandlerInvocation", token, action }` batch entries in `dispatch_component_actions_with_limits`: unknown token error, action-type check against the handler's `action_name`, live-state override of `owner_state` captures before evaluation (design D5), routing of results into working state or effects, and re-render plus token walk after the batch. Verify tests for: single invocation patches state and returns rendered output; two increments in one batch reach `2`; a later handler sees an earlier patch; captured loop variables keep their snapshot; mixed results route item by item; parent-bound handler update records come back as effects.
- [x] 3.6 Make the batch atomic: any error returns `Err` before encoding. Verify a test where a valid first entry is followed by an unknown token leaves the original snapshot usable with its original state, and an update with a wrong-typed field (constructed by bypassing static analysis) aborts the batch.
- [x] 3.7 Extend `ComponentDispatchResult` in `crates/nx-interpreter` and `crates/nx-api` with `rendered`, add the invocation shape to `validate_dispatch_action_input`, and update `crates/nx-api/src/value.rs` so `to_nx_value` emits the token form and `from_nx_value` accepts update records with absent fields. Verify `cargo test -p nx-api` covers a JSON dispatch result containing `rendered`, `effects`, `state_snapshot`.
- [x] 3.8 Add a `crates/nx-interpreter/tests/update_records.rs` end-to-end suite that drives init → dispatch → dispatch through the public `Interpreter` API for a `Counter` and a `Form` that re-emits `Saved`. Verify it passes and covers every scenario in the component-runtime-bindings delta.
- [x] 3.9 Construct host-supplied records at every depth (design D2): thread a `ValueOrigin` through `coerce_value_to_resolved_type`, `build_record_value_from_shape`, `build_update_record_value`, and `build_union_case_value`, so a record inside props, explicit state, or a batch entry is rebuilt against its declaration while interpreter-produced values keep the by-name check. A host value whose `$type` names a component is built against that component's props, and an action entry is constructed before its handler is looked up. Verify tests in `tests/update_records.rs` for an update record in a prop (kept, unknown field, non-nullable null), in a prop array, in an action payload, in explicit state, under a component-typed prop, and in an action entry with no bound handler, and a nested plain record missing a required field; and a .NET test that a drifted prop DTO fails initialization naming the field.

## 4. FFI, C header, and .NET binding

- [x] 4.1 Update `crates/nx-ffi` dispatch to pass batches with invocation entries and to return `rendered` in both output formats; regenerate `bindings/c/nx.h` comments if the payload documentation changed. Verify `crates/nx-ffi/tests/ffi_smoke.rs` gains a dispatch-with-token case that asserts all three result parts.
- [x] 4.2 Change `NxComponentDispatchResult<TEffect>` to `NxComponentDispatchResult<TRendered, TEffect>` with `Rendered`, add `NxActionHandlerRef` and `NxHandlerInvocation<TAction>`, and update every `DispatchComponentActions*` overload in `NxRuntime.cs`. Verify a new `NxUpdateRecordTests.cs` gains the typed Counter scenario (token read from rendered element, dispatched, `Rendered` shows `1`, `Effects` empty) and the stale-token exception scenario.
- [x] 4.3 Add `NxOptional<T>` with a `System.Text.Json` converter and a MessagePack formatter that omit unset values and read missing keys as unset. Verify new serialization tests round-trip a DTO with one unset and one null property through both formats, and that a MessagePack-encoded update passed as a prop decodes natively with only the present field.
- [x] 4.4 Run `dotnet test bindings/dotnet` and the FFI suite. Verify both pass.

## 5. NX IR and the TypeScript runtime

- [x] 5.1 In `crates/nx-codegen`, emit IR declarations for referenced update records as `tag: "record"` declarations carrying `updateTarget` and self-contained optional field schemas (design D8); add `NX_IR_REQUIRED_FEATURE_UPDATE_RECORDS_V1` when any is emitted. Verify `crates/nx-codegen/src/tests.rs` covers: referenced record declared with the expected schema, unreferenced record absent with unchanged features, and `<Counter.Update count=1 />` encoded as record construction.
- [x] 5.2 In `runtime/typescript/src/index.ts`, accept the feature, route update-kind declarations to a patch normalizer, and let `applyComponentStatePatch` accept a `Component.Update` value with a `$type` target check. Verify `runtime/typescript/test/runtime.test.ts` covers the five TypeScript-runtime scenarios and `emitted-ir.test.mjs` runs a program that constructs an update record.
- [x] 5.3 Run `pnpm -r test` for `runtime/typescript`. Verify it passes.

## 6. typegen

- [x] 6.1 In `crates/nx-cli/src/typegen/model.rs`, export a `<Name>_update` companion for every exported record, action, and stateful component, reusing the `_state` collision warning-and-skip logic. Verify a model test lists the companion with optional fields and no props for a component.
- [x] 6.2 Emit the TypeScript companion (`export interface User_update` with `$type: "User.Update"`, optional properties, `| null` where nullable). Verify typegen tests in `crates/nx-cli/src/typegen.rs` assert the exact property forms and the collision skip.
- [x] 6.3 Emit the C# companion with `NxOptional<T>` properties and dual wire-name attributes. Verify typegen tests assert the property types and attributes, and that a generated file compiles in the .NET test project alongside 4.3.

## 7. Documentation and examples

- [x] 7.1 Update `docs/src/content/docs/reference/syntax/functions.md`: replace the "state-update actions deferred" runtime note, document `T.Update`, the absent-versus-null rule, the bare `Update` form, handler result routing, live state reads, and batch semantics, with a `Counter` example. Verify the page renders in the docs build (`pnpm --filter docs build` or the equivalent) with no warnings.
- [x] 7.2 Update `docs/src/content/docs/language-tour/functions.md` guidance bullets and add a short patch example to `docs/src/content/docs/reference/syntax/elements.md` where element-shaped construction is described. Verify the docs build passes.
- [x] 7.3 Add a prose note to `nx-grammar.md` (Components section) that `Update` and `<Name>.Update` are derived names rather than grammar productions. Verify the note appears next to the `EmitsGroup` description.
- [x] 7.4 Extend `examples/nx/component.nx` with a `Counter` that updates its own state and re-emits `Reset`, and copy it into a checker test in `crates/nx-types/tests/type_checker_tests.rs` under the documentation-tests section. Verify the example passes analysis and the interpreter suite from 3.8 runs it.

## 8. Integration verification

- [x] 8.1 Run the full workspace test suite (`cargo test --workspace`, `pnpm -r test`, `dotnet test`). Verify all pass.
- [x] 8.2 Run `openspec validate add-update-actions --strict`. Verify it reports no issues.
