## 1. HIR: derived property unions

- [x] 1.1 Add `property_target: Option<Name>` to `UnionDef` in `crates/nx-hir/src/lib.rs` with `PROPERTY_UNION_SUFFIX`, `property_union_name`, `is_property_union_name`, and `UnionDef::property_target()` beside the update-record helpers; thread the new field through every `UnionDef` constructor. Verify `cargo build --workspace` is clean and existing union tests pass unchanged.
- [x] 1.2 In `crates/nx-hir/src/lower.rs`, synthesize `<Name>.Property` wherever `predeclare_update_record` runs (records, actions, inline emits, components with state), building one fieldless case per effective field (inherited first) from the same shape the update record uses, registering it in the Type namespace and appending it through the same pending list after authored items. Verify lowering tests show `User.Property` and `Counter.Property` as `Item::Union` with `property_target` set, the expected case order for `abstract type Named = { name:string } type User extends Named = { email:string }`, and no props on a component's union; verify no existing test that indexes items by position changes.
- [x] 1.3 Reject an inline emit or emit reference named `Property` with the reservation diagnostic, and reject `extends` targeting a property union. Verify lowering tests for both `component-syntax` scenarios and the "cannot be extended" scenario report the diagnostics.
- [x] 1.4 Add the bare `Property` rewrite (design D2) for a single-segment type reference and for a member-access base inside `current_component`, leaving it untouched outside. Verify lowering tests for a state default `Property.count`, a handler body `Property.name`, a root-level `Property.name` left as written, and precedence over a declared `type Property`.

## 2. Type checking

- [x] 2.1 Register synthesized property unions in `register_type_definitions` like any local or imported union so `Type::Union` resolution, bare contextual names, match exhaustiveness, and `==` work without new branches; add the "unknown-property-union" and "bare-property-outside-component" diagnostics beside `report_unresolved_update_tag`, including the nested-name wording for an `X` ending in `.Update` or `.Property`. Verify a new `crates/nx-types/tests/property_references.rs` covers every scenario in the `property-references` spec's first three requirements, including the imported-record scenario through a workspace fixture.
- [x] 2.2 Add the intrinsic table and the pre-lookup branch in the `Call` arm of `infer_expr` (design D4) with the four typing rules, origin-based same-`T` checks, arity errors, and the reserved-name rejection in `register_function_signatures`. Verify `crates/nx-types/tests/update_intrinsics.rs` covers every type-checking scenario in the `update-records` delta, including the parameter-shadowing and redeclaration scenarios.
- [x] 2.3 Run `cargo test -p nx-hir -p nx-types` and fix any fixture that changes because the four names are now reserved or because `X.Property` no longer reads as an unknown host element. Verify both suites pass and note each fixture changed in the commit message.

## 3. Interpreter and API

- [x] 3.1 Dispatch the intrinsics by flattened callee name in `eval_call` before `resolve_item` (design D4): `apply` through the effective shape and `coerce_update_field_value`, `merge` right-biased, `diff` with the `==` equality, `changed` in shape order yielding `Value::UnionCase { union: "T.Property", case }`. Verify inline interpreter tests cover every evaluation scenario in the `update-records` delta, including a present `null` through `apply`, `merge`, and `diff`, and `apply(diff(a, b))` equal to `b` for records with nested records and lists.
- [x] 3.2 Confirm host input for a `T.Property`-typed prop lifts a bare string to the union case through `coerce_value_to_resolved_type` and rejects an unknown name with the union and field named; confirm `to_nx_value` encodes the case and a `changed` result as bare strings. Verify tests in `crates/nx-api` for the three wire-form scenarios in the `property-references` spec, JSON and MessagePack.
- [x] 3.3 Add an end-to-end suite `crates/nx-interpreter/tests/property_references.rs` driving a component whose state holds a `Property` value and whose handler returns `<Update sortBy={Property.name} />`. Verify init and dispatch round-trip the case through a snapshot and the rendered output carries `"name"`.

## 4. NX IR, generated JavaScript, and the TypeScript runtime

- [x] 4.1 In `crates/nx-codegen`, add `property_target` to the IR and model union kinds, emit only referenced property unions (discovery beside `referenced_update_records`), add `NX_IR_REQUIRED_FEATURE_PROPERTY_UNIONS_V1 = "property-unions-v1"`, and encode a case with the existing `unionCase` op. Verify `crates/nx-codegen/src/tests.rs` covers the three declaration scenarios in the `nx-ir-format` delta and that a program without property references produces byte-identical IR.
- [x] 4.2 Add the `intrinsicCall { intrinsic, args }` op and `NX_IR_REQUIRED_FEATURE_UPDATE_INTRINSICS_V1 = "update-intrinsics-v1"`, lowering checker-resolved intrinsic calls to it. Verify a codegen test asserts the op shape and the feature for `apply(...)`, and that a program calling a user function named otherwise still emits a plain `call`.
- [x] 4.3 Add `applyUpdate`, `mergeUpdates`, `diffRecords`, and `changedFields` to the runtime helper module in `crates/nx-codegen/src/runtime.rs`, reserve the names, and emit `intrinsicCall` as calls to them in both JavaScript and TypeScript output; emit a referenced property union as a constant union under the naming the emitter already uses for dotted declarations. Verify the three scenarios in the `executable-code-generation` delta pass through the node harness and the interpreter comparison, and the TypeScript output type-checks under the `tsc` harness.
- [x] 4.4 In `runtime/typescript/src/index.ts`, accept both features and the new op tag, validate and evaluate `intrinsicCall`, keep constant-case normalization for property unions, and export the four helpers with a shared record type parameter. Verify `runtime/typescript/test/runtime.test.ts` covers every scenario in the `typescript-ir-runtime` delta and `emitted-ir.test.mjs` runs an emitted program that calls `apply` and reads `User.Property.email`.
- [x] 4.5 Run `pnpm -r test` for `runtime/typescript` and `cargo test -p nx-codegen`. Verify both pass with `node` present, and note whether `node` was available in the run.

## 5. typegen

- [x] 5.1 In `crates/nx-cli/src/typegen/model.rs`, export a `<Name>_property` companion for every exported record, action, and stateful component as a constant `ExportedType::Union` with `companion_target` set (design D6), reusing the collision warning-and-skip logic, and rewrite `X.Property` type references to the companion including through imported libraries. Verify model tests list the companion with cases in effective order, skip on collision, exclude component props, and resolve an `X.Property` prop type on an imported record to the dependency's companion.
- [x] 5.2 Confirm the TypeScript and C# emitters produce the companion through the unchanged constant-union path. Verify typegen tests in `crates/nx-cli/src/typegen.rs` assert `export type User_property = "name" | "email";`, the C# `enum User_property` with the wire-format class and enum helper attributes, and the `sortBy` / `columns` prop types from the `cli-code-generation` delta.
- [x] 5.3 Extend `bindings/dotnet/tests/NxLang.Sdk.Tests/Generated/update-records.nx` with a `T.Property`-typed field, regenerate `UpdateRecords.g.cs`, and add a .NET test that round-trips the enum through JSON and MessagePack as the bare field name. Verify `dotnet test bindings/dotnet` passes and the nx-cli drift test still matches the checked-in file.

## 6. Documentation and examples

- [x] 6.1 Update `docs/src/content/docs/reference/syntax/functions.md` ("Updating state") and `reference/syntax/types.md` ("Constant Unions") to document `T.Property`, the bare `Property` form, the reserved names, and the four intrinsics with a `Counter` example; add the reservation note next to the existing `Update` note. Verify the docs build (`pnpm --filter docs build`) passes with no warnings.
- [x] 6.2 Add a short property-reference paragraph to `docs/src/content/docs/language-tour/functions.md` and `language-tour/types.md`. Verify the docs build passes.
- [x] 6.3 Extend the derived-names prose note in `nx-grammar.md` (Components section) to cover `Property` and `<Name>.Property`, and add a sentence under Types that a property union is a constant union. Verify the note sits beside the existing `Update` paragraph.
- [x] 6.4 Extend `examples/nx/component.nx` with a `sortBy:Counter.Property` state field set through `<Update sortBy={Property.count} />` and a `changed(...)` use, and extend `test_component_example_with_update_records` in `crates/nx-types/tests/type_checker_tests.rs` to assert `Counter.Property` is an `Item::Union`. Verify the example passes analysis and the interpreter suite from 3.3 runs it.

## 7. Integration verification

- [x] 7.1 Run `cargo test --workspace`, `pnpm -r test`, and `dotnet test bindings/dotnet`. Verify all pass.
- [x] 7.2 Run `openspec validate add-property-references --strict`. Verify it reports no issues.
