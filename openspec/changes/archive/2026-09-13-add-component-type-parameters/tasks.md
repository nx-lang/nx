## 1. Grammar and validation

- [x] 1.1 Add parser tests in `crates/nx-syntax/tests/parser_tests.rs` pinning that a prop named `type` still parses in a component signature (`component <X type:string /> = { <a /> }`) and in a record (`type X = { type:string }`); verify they pass on the current grammar before any change.
- [x] 1.2 In `crates/nx-syntax/grammar.js`, allow the `'type'` keyword as an alternative to the `type` rule for the type field of `_component_property_definition` and `property_definition`; regenerate with `pnpm generate` in `crates/nx-syntax`; verify the 1.1 tests still pass and `external component <SkiaLayout extends SkiaControl TItem:type layoutType:string? itemsSource:TItem[]? />` parses to three PROPERTY_DEFINITION nodes with the first carrying the keyword token (the `component-syntax` leading-type-parameters scenario).
- [x] 1.3 In `crates/nx-syntax/src/validation.rs`, reject a `type`-typed definition that follows a regular prop, carries a default, carries a modifier, or appears outside a component signature (record, action, emitted action, state group), each with a targeted message naming the definition; verify with validation tests for the `component-syntax` rejection scenarios and confirm `TItem:type?` is a parse error and `let f(T:type)` is rejected by validation.

## 2. HIR: type parameters as their own list

- [x] 2.1 Add `TypeParameter { name, span }` and `Component.type_params: Vec<TypeParameter>` in `crates/nx-hir/src/lib.rs`, and split `type`-typed definitions out of `props` in `lower.rs`; verify with a lowering test that `<SkiaLayout TItem:type itemsSource:TItem[]? />` lowers to one type parameter and one prop.
- [x] 2.2 Extend `EffectiveComponentContract` in `crates/nx-hir/src/components.rs` with `type_params` (inherited first, each carrying its declaring module identity) and reject a derived type parameter or prop that duplicates an inherited type parameter alongside the existing duplicate-prop check; verify with contract tests for the `component-contract-inheritance` "adds its own" and "redeclaring is rejected" scenarios.
- [x] 2.3 Add `erase_type_parameters(&ast::TypeRef, &[Name]) -> ast::TypeRef` (parameter names become `object`, `Array`/`Nullable` preserved) and `remove_property_entries(&mut PreparedModule, &FxHashSet<ExprId>)` (drops plain `Value` entries by value expression id) next to `apply_contextual_name_resolutions`; verify each with a unit test.

## 3. Checker: declaration scope and use-site substitution

- [x] 3.1 Add `Type::Parameter(TypeParameterRef { name, owner, ordinal })` in `crates/nx-types/src/ty.rs` with `Display` as the name, satisfaction rules (itself and the bottom type satisfy it; it satisfies itself and `object`; composes under `?`/`[]`), join as mismatch, and a `substitute` walk; verify with unit tests in `ty.rs` and `semantics.rs` covering each rule.
- [x] 3.2 In `infer_component`, push a type scope mapping each effective type parameter name to its `Type::Parameter` before resolving prop and state annotations, defaults, and the body, and make `resolve_named_type` consult that scope first; verify with tests for the `component-type-parameters` "rigid nominal type" scenarios: concrete value rejected, two parameters distinct, `object`/empty list accepted, shadowing, not visible at top level, and not a value in the body.
- [x] 3.3 In `check_element_bindings_against_component`, partition type-argument entries from value entries, reject conditional and non-bare-name arguments with diagnostics that show the `Name=` form, resolve bare names through the type-name resolver with a near-match suggestion on failure, and build the substitution map with `never` for every unbound parameter; verify with tests for the "supplies a type argument" scenarios including forwarding an enclosing parameter, the value-binding-of-the-same-name case, and the unknown-property case on a component without parameters.
- [x] 3.4 Build the `ElementBindingSpec` from substituted prop types, recording per entry the unspecified parameter its type mentioned, and add the "typed by 'TItem', which was not specified; add TItem=" branch to `check_property_path_bindings` so it replaces the mismatch only after a quiet satisfaction test fails; verify with tests for every "unspecified type parameter" scenario, including one element carrying both an unspecified-parameter failure and an unrelated ordinary mismatch.
- [x] 3.5 Record consumed type-argument expression ids and a per-element map of resolved arguments in the inference context, call `remove_property_entries` from `check.rs` beside the contextual-name rewrite, and return the resolved-argument map with the type environment in the analysis result; verify with a checker test that the snapshotted module's element has no `TItem` entry while the analysis result maps that element to `TItem = Contact`, and with the "elements with different arguments have the same type" scenario.
- [x] 3.6 Extend the contextual-literal diagnostics so a bare name at a type-parameter site reports "expects a type name" with a suggestion, and add the new `unbraced-literal-forms` scenarios (visible type resolves despite a same-named `let`; `Contatc` suggests `Contact`) to `crates/nx-types/tests/contextual_literals.rs`; verify they pass and the existing union-case scenarios are unchanged.
- [x] 3.7 Add inheritance coverage in `crates/nx-types/tests`: an argument for an inherited parameter, a derived body seeing the inherited parameter, and a base declared in a second module resolved from a derived component in the first; verify all pass.

## 4. Runtime, IR, and generated surfaces

- [x] 4.1 Add an interpreter test in `crates/nx-interpreter/tests` that checks and evaluates `<SkiaLayout TItem=Contact itemsSource={} />` and asserts the external component record has `itemsSource` and no `TItem`, including in its JSON serialization; verify it passes with no interpreter code change.
- [x] 4.2 Carry `type_params` on `CodegenComponent` in `crates/nx-codegen`, apply `erase_type_parameters` in the IR builder and in the TypeScript `Element` emission, and emit `Props` and the factory as generic with `= unknown` defaults; verify with IR tests for the `nx-ir-format` scenarios (prop schema is a nullable list of `object`, no `TItem` entry, descriptor has no `TItem` property) and with an executable-output test for the `external-components` scenario (`SkiaLayoutProps<TItem = unknown>` types `itemsSource` as `TItem[] | undefined`, `SkiaLayoutElement` uses `unknown[]` and has no parameter, `SkiaLayout({})` returns `$type` `"SkiaLayout"`, and the emitted TypeScript passes `tsc`).
- [x] 4.3 Carry the component's type parameters on the exported contract in `crates/nx-cli/src/typegen/model.rs`, apply `erase_type_parameters` in the C# emitter, and emit the TypeScript contract type as generic with `= unknown` defaults; verify with typegen tests for the `cli-code-generation` C# (nullable list of `object`, no generic parameter, no `TItem` member) and TypeScript (`SkiaLayout<TItem = unknown>` with `itemsSource?: TItem[]`, no `TItem` member) scenarios.
- [x] 4.4 Add a language-service smoke test in `crates/nx-language-service/src/lib.rs` that loads a generic component with a use site, requests diagnostics, hover on `TItem` in the signature, and completions inside the body; verify no panic and that hover renders the parameter as `TItem`.

## 5. Docs and integration

- [x] 5.1 Add a "Type parameters" section to the component documentation under `docs/src/content/docs` (the page that documents component declarations, beside content props and defaults) covering the declaration form, the `TItem=Contact` use-site form, the "leave it out when nothing needs it" rule, and the naming convention (`T` or a `T`-prefixed PascalCase name); verify the docs build with the docs project's build command.
- [x] 5.2 Run `cargo fmt --all`, `cargo clippy --workspace --all-targets`, and `cargo test --workspace`; verify all green and that no test outside this change needed its expectations altered except the ones the specs modify.
