## REMOVED Requirements

The `playground` capability supersedes this one. Every requirement below is carried forward there
under the playground's name, with the fiddle's URL scheme, branding and deployment replaced by the
site's own. The `openspec/specs/drawnui-fiddle` directory is deleted when this change is archived.

### Requirement: Gallery presents the DrawnUI example set
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Gallery presents the DrawnUI example set".

### Requirement: Every gallery entry opens in the fiddle
**Reason**: Superseded by the `playground` capability; the fiddle view is now the editor view at
`/playground/<id>`.
**Migration**: See `playground` — "Every gallery entry opens in the editor view".

### Requirement: Examples declare how completely they cover the original
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Examples declare how completely they cover the original".

### Requirement: Missing capabilities are named from a shared vocabulary
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Missing capabilities are named from a shared vocabulary".

### Requirement: Example source marks where dropped behavior belonged
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Example source marks where dropped behavior belonged".

### Requirement: Every gallery entry is backed by working NX
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Every gallery entry is backed by working NX".

### Requirement: A static example rests in a sensible state
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "A static example rests in a sensible state".

### Requirement: Example NX is authored against the catalog
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Example NX is authored against the catalog".

### Requirement: Two-pane fiddle renders authored NX
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Two-pane editor view renders authored NX".

### Requirement: Source pane is an NX-aware editor
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Source pane is an NX-aware editor".

### Requirement: Compilation reports diagnostics against authored source
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Compilation reports diagnostics against authored source".

### Requirement: Catalog is available to authored source without being shown
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Catalog is available to authored source without being shown".

### Requirement: Evaluated NX values are translated to drawn controls
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Evaluated NX values are translated to drawn controls".

### Requirement: Interaction is limited to DrawnUI's own behavior
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Interaction is limited to DrawnUI's own behavior".

### Requirement: Compilation runs on the server behind a replaceable boundary
**Reason**: Superseded by the `playground` capability.
**Migration**: See `playground` — "Compilation runs on the server behind a replaceable boundary".

### Requirement: Application is deployable as a single service
**Reason**: Superseded by the `playground` capability, which adds committed deployment
configuration and the health check.
**Migration**: See `playground` — "Site is deployable as a single service".

### Requirement: Sample app documents how to run and sync it
**Reason**: Superseded by the `playground` capability, which adds deployment documentation.
**Migration**: See `playground` — "Site documents how to run, sync and deploy it".
