## 1. Prelude: build and binding

- [x] 1.1 Add the reserved prelude identity constant (`@nx/prelude.nx`) to `nx-hir`, add `crates/nx-api/src/prelude.nx` declaring `export type Range = { T:type start:T end:T endInclusive:boolean }`, and refuse a workspace module under that identity in `LogicalModuleGraph::from_modules`; verify with unit tests that identity normalization leaves it unchanged and that a workspace supplying it fails with a diagnostic naming it
- [x] 1.2 Split `build_library_artifact_with_registry` so the directory read is separate from the build and the build accepts in-memory sources; verify the existing library tests in `artifacts.rs` pass unchanged
- [x] 1.3 Build the prelude once per process into a `LibraryArtifact` behind a `OnceLock`, embedded with `include_str!`; verify with a test that it has no diagnostics, exports `Range`, `Range.Update` and `Range.Property`, and carries its source in `sources`
- [x] 1.4 Add `apply_prelude_bindings` and call it last in both `prepare_logical_source_file` and `prepare_library_source_file`, skipping the prelude itself and taking no part in the ambiguity and duplicate-import checks; add the prelude to the program's libraries; verify with tests beside the `// Implicit imports` block for each scenario under "The prelude is part of every build" — single source, two-module workspace, library directory
- [x] 1.5 Verify shadowing with tests for each scenario under "Prelude names bind last and never conflict": a local `Range`, a wildcard-imported `Range` with no "provided by both", a host implicit import exporting `Range`, and the pre-existing generic `type Range` from `record-type-parameters`
- [x] 1.6 Hash the prelude's fingerprint into the program fingerprint, and keep prelude declarations out of `build_interface_item`'s exports; verify the "A library does not export the prelude" and "Positions are the document's own" scenarios, and that a label inside the prelude carries the prelude's identity and renders the prelude's source line without a disk read
- [x] 1.7 Run `cargo test --workspace` and verify every existing test, including `crates/nx-types/tests/record_type_parameters.rs` and the IR conformance corpus, passes with no expected-file changes

## 2. Grammar and lowering

- [x] 2.1 Add `..` and `..=` to `binary_expression` in `grammar.js` at `prec.left(100)`, run `pnpm generate` in `crates/nx-syntax`, and update `syntax_kind.rs`; verify `tree-sitter generate` reports no new conflicts
- [x] 2.2 Add parser tests beside `test_binary_expressions_arithmetic` for `1..5`, `1..=5`, `0..n + 1`, `-5..-1`, `page.first..=page.last`, `1.5..2.5`, `1..5..9`, a range as a `for` iterable in both forms, a range as a property value, and a parenthesized range in a braced list; verify each tree's operator field and grouping, and that `1..5` holds two `int_literal` nodes
- [x] 2.3 Add `Expr::Range { start, end, inclusive, span }` to `nx-hir` and lower both operators to it; verify with a lowering test for each operator
- [x] 2.4 Add `..` and `..=` to the operator list in `queries/highlights.scm`; verify with a case in `crates/nx-syntax/tests/query_tests.rs`

## 3. Type checking

- [x] 3.1 Type `Expr::Range` by the rule in design D3 — prelude `Range` required, expected-type operands checked as field bindings, otherwise `numeric_promotion` — with diagnostics `range-operand-not-numeric` (pointing to the element form) and the existing no-common-type error; verify with a new `crates/nx-api/tests/ranges.rs` covering every scenario under "A range expression constructs the prelude's `Range`" that is a checking scenario
- [x] 3.2 Report `range-hidden`, labelling the hiding declaration or import, when `Range` in the module is not the prelude's, and a plain "prelude is not available" error when there is no prelude at all; verify both scenarios under "A range expression is rejected where `Range` is not the prelude's", plus one `nx-types` test for the no-prelude case
- [x] 3.3 Extend the `Expr::For` arm to accept the prelude `Range` with an integer argument, bind the item as that type and the index as `int`, reject other ranges with `range-not-iterable`, and record range-driven `for` expressions; verify the checking scenarios under "`for` iterates a range of an integer type" and that `crates/nx-types/tests/for_loop_names.rs` still passes
- [x] 3.4 Add `nx_hir::apply_range_constructions` to the post-check block in `check.rs`, replacing each `Expr::Range` with a `Range` record literal that keeps the `ExprId`, and recording the allocated boolean literal's type; verify with a test that no `Expr::Range` survives in a checked module and that a range in a `let`, a property value, a function argument, a list and a `for` header each rewrite
- [x] 3.5 Join two applied types by their invariance in `InferenceContext::common_supertype` — the same instantiation for two of one, `object` otherwise — so no inferred type is ever a generic record's bare name, and restate the `record-type-parameters` invariance requirement with the two list scenarios; verify with `crates/nx-types/tests/record_type_parameters.rs` cases for a list of one instantiation, a list of two, and an instantiation beside an unrelated record

## 4. Interpreter

- [x] 4.1 Make `eval_for` iterate a `Range` record by count without building a list, handling empty, reversed, one-element closed and maximum-value closed ranges and keeping the start's integer carrier; verify in `crates/nx-api/tests/ranges.rs` every evaluation scenario under "`for` iterates a range of an integer type" and "A range expression constructs the prelude's `Range`", including `1..=5 == <Range … />` and `1..5 != 1..=4`
- [x] 4.2 Verify with a test that `for i in 0..2000000 { i }` fails with `OperationLimitExceeded` under default limits rather than exhausting memory, and that a host-supplied `Range` value iterates

## 5. NX IR and executable code

- [x] 5.1 Give the prelude library module its reserved identity in the IR builder instead of a path, emit it when named and, for an every-module request, when some module references it, and check the image file name derived from the identity is valid on every platform; verify with `ir_tests.rs` cases for each scenario under "The prelude is an ordinary NX IR module", and with a CLI test that `nxlang codegen --target nx-ir` writes and `nxlang ir explain` re-reads the prelude image
- [x] 5.2 Add node kind `22 forRange` and `NX_IR_REQUIRED_FEATURE_RANGES_V1`, emit them for recorded range-driven `for` expressions, and render the node in `explain`; verify each scenario under "NX IR encodes iteration over a range as its own node behind a required feature", including that `NX_IR_SCHEMA_VERSION` is still 4
- [x] 5.3 Add a `specs/ir-conformance/ranges/` program — both operators, equality, an indexed loop, an empty loop, a float range passed through, and a second module that iterates a range built by the first — register `forRange` coverage in `manifest.json`, and regenerate with `NX_UPDATE_CORPUS=1 cargo test -p nx-codegen --lib ir_corpus`; verify the size budget holds and that no other program's `expected/` files changed
- [x] 5.4 Emit `nxRangeMap` for a range-driven `for` in `crates/nx-codegen/src/emit.rs` and collect the helper; verify with a case in `crates/nx-codegen/src/tests.rs` beside the `for`-loop end-to-end test that the emitted module type-checks under `tsc` and produces the interpreter's values
- [x] 5.5 Add an `nx-codegen` test that emits the prelude image and compares it with `runtime/typescript/src/prelude-image.ts`, rewriting the file under `NX_UPDATE_PRELUDE_IMAGE=1` and otherwise failing with that command in its message; generate the file; verify the test fails after an edit to `prelude.nx` and passes after regenerating
- [x] 5.6 Update `docs/nx-ir-format.md`: the node table, the required-features list, the module section (the prelude's identity, its empty version, when it is emitted), and the linking section (the runtime's built-in prelude and how a host overrides it)

## 6. TypeScript IR runtime

- [x] 6.1 Wrap the host resolver in `tryLinkNxIrProgram` so the prelude's identity falls back to the built-in image from `prelude-image.ts`, prepared once at module scope, and give `prepareNxIrProgram` the same fallback; verify with `runtime.test.ts` cases for each scenario under "TypeScript runtime supplies the prelude module itself"
- [x] 6.2 Add `ranges-v1` to `knownFeatures` and evaluate `forRange` as a counting loop that validates a `Range` record with safe-integer bounds; verify with `runtime/typescript/test/runtime.test.ts` cases for each scenario under "TypeScript runtime iterates a range without building it", and that `corpus.test.mjs` passes the new program
- [x] 6.3 Add `maxRangeLength` to `NxRuntimeOptions` with a default of 1,000,000, failing with `nx-ir-resource-limit` before the body runs; verify the oversized-range scenario at the default and with a raised limit
- [x] 6.4 Verify an image that lists `ranges-v1` is refused by name when the feature is removed from `knownFeatures` in a test double, and document the option and the feature in `runtime/typescript/README.md`

## 7. Generated contracts and the .NET SDK

- [x] 7.1 Give the typegen export model the prelude library as a source of records, and resolve a type name through the module's own namespace before the prelude
- [x] 7.2 C#: map a name that resolves to the prelude's `Range` to `global::NxLang.Nx.NxRange<…>` beside the `NxFunctionRef` arm and emit no declaration for it; verify with tests in `crates/nx-cli/src/typegen.rs` for each C# scenario in the `cli-code-generation` delta
- [x] 7.3 TypeScript: emit referenced prelude records through the ordinary record emitter into `_nx.ts` for library output, with imports and an `index.ts` re-export, and inline for single-file output; verify each TypeScript scenario in the delta, including unchanged output when no prelude type is referenced
- [x] 7.4 Add `NxRange.cs` to `bindings/dotnet/src/NxLang.Sdk` with value equality and the JSON and MessagePack wire shape of an equivalent generated record; rebuild `nx-ffi`, then verify with `dotnet test bindings/dotnet/NxLang.sln` the round-trip, evaluated-range and prelude-tracking scenarios in the `dotnet-binding` delta

## 8. Editor and sites

- [x] 8.1 Add a `\.\.=?` operator pattern ahead of the dot rule in `src/vscode/syntaxes/nx.tmLanguage.json`; verify with `pnpm test:grammar` cases for each scenario in the `editor-syntax-highlighting` delta
- [x] 8.2 Verify in the language-service tests that a one-document snapshot offers `Range` in a type position and as an element tag, that hover on it is labelled a built-in type — as `Element` is — while showing its real declaration with its type parameter and fields, and that an LSP snapshot built with no build context does the same
- [x] 8.3 Make `classify` in `sites/playground/src/compile/catalog.ts` treat a label in the prelude's identity as not the visitor's text; verify with a unit test, then verify in the running playground, with its resolver unchanged, that `for i in 0..5 { … }` renders five items, that a catalog prop typed `<Range T=float64/>` accepts `0..1`, and that `1..` reports a diagnostic at the visitor's own position

## 9. Retire the text-prepending prelude

- [x] 9.1 Add `implicitImports` to the Node SDK's `NxLanguageSnapshotOptions` and plumb it through `bindings/node/native` to the snapshot's build context; verify with `bindings/node/test/language-snapshot.test.ts` cases for both scenarios in the `sdk-node` delta
- [x] 9.2 Replace the `prelude` option of `createSnapshotLanguageService` and `createNxLanguageHandler` with `context: { documents, implicitImports }`, and delete `packages/language-core/src/prelude.ts`, `PRELUDE_ORIGIN`, the shifting in `answer.ts`, their exports and `test/prelude.test.ts`; verify with `handler.test.ts` and `service.test.ts` cases for each scenario under "Host context is served as implicitly imported documents", and that passing `prelude` is a construction-time error naming `context`
- [x] 9.3 Switch the reference handler in `bindings/wasm/test/language-service.test.ts` to `context` and verify the wasm and HTTP answers still agree; update `packages/language-client/test/e2e.test.ts` if it passes `prelude`
- [x] 9.4 Rewrite the "Preludes" sections of `packages/language-core/README.md` and `packages/language-http/README.md` and both package descriptions for `context`, with the migration from `prelude`; verify `grep -rn -i prelude packages/*/src bindings/*/src` finds only the two construction-time guards that name the removed option and the .NET SDK's `NxRange` and its `NxRangeUpdate` / `NxRangeProperty` companions, which name the language prelude

## 10. Documentation

- [x] 10.1 Add "Counting with a range" to `reference/syntax/for.md` and the two operators, with their precedence, to the operator table in `reference/syntax/expressions.md`
- [x] 10.2 Add a "Ranges" section to `reference/syntax/types.md` that introduces `Range` as a built-in type, covering `Range`, the operators as sugar for it, how `T` is chosen, and that only integer ranges iterate; change the "Generic records" examples that declare their own `Range` to refer to the built-in one; in this page and in `for.md` say "built-in", not "prelude"
- [x] 10.3 Add "Built-in declarations" to `reference/syntax/modules.md`, which carries the prelude's full source, and a use of it to the language tour's modules page, introducing the term once — built-in types are declared in the NX prelude, a module every file imports automatically — then: that the prelude lives under the reserved `@nx/` root, that its names are shadowable, and that shadowing `Range` disables the operators in that module
- [x] 10.4 Update `nx-grammar.md` (`BinaryExpression`) and `nx-grammar-spec.md` (tokens, precedence level 100, the disambiguation note for `1..5`); verify both match `grammar.js`
- [x] 10.5 Rewrite the integer-range-type note in `specs/future.md` to the `1..=5` spelling and record membership, clamping, step and range patterns as follow-ups
- [x] 10.6 Rename the record in `examples/nx/generic-records.nx` so the example no longer shadows the prelude, add `examples/nx/ranges.nx`, and verify `nxlang run` accepts both

## 11. Verification

- [x] 11.1 Run `cargo test --workspace`, `cargo clippy --workspace`, `cargo fmt --all --check`, the TypeScript runtime, wasm and Node SDK tests, the grammar tests and `dotnet test`, and verify all pass
- [x] 11.2 Run `openspec validate add-range-type-and-operators --strict` and verify it passes

## 12. Review follow-up: RF6, RF8, RF10, RF14, RF20

- [x] 12.1 Give the prelude a contract version: add `nx_hir::PRELUDE_VERSION`, stamp it on the prelude's module-table entry in `module_version`, re-export `NX_PRELUDE_VERSION` from the runtime's generated image module, and regenerate `prelude-image.ts` and the conformance expectations; verify with `the_prelude_declaration_shape_is_pinned_to_the_prelude_version`, the version assertion in `a_range_expression_links_against_the_prelude`, and the two runtime cases for an image compiled against another prelude contract (RF10)
- [x] 12.2 Replace typegen's graph-wide prelude lookups with `ModuleTypes`, the export graph as one module resolves names against it — a declaration hides a prelude name library-wide, an import only in its own module — and thread it through both emitters, covering `record_type_params` as well as `prelude_record`; verify with `an_import_of_a_foreign_range_leaves_its_sibling_with_the_prelude_s` (RF8)
- [x] 12.3 Generate the prelude's derived companions: keep them in `prelude_declarations()`, emit them beside `Range` in TypeScript, and add `NxRange_update<T>`, `NxRange_property` and `NxRangeProperties<T>` to the .NET SDK; verify with `a_field_typed_by_a_prelude_companion_generates_in_both_languages`, the checked-in `PreludeCompanions.g.cs` fixture and `NxRangeCompanionTests` (RF6)
- [x] 12.4 Make a C# member typed by a generic update companion compile: declare a closed formatter for each instantiation a member's own type is and name it from the member, and warn for the shapes MessagePack cannot resolve — a companion the module declares, or one nested inside another type; verify with `a_generic_update_companion_csharp_cannot_format_is_reported` and the compiled fixture (RF6)
- [x] 12.5 Update `docs/nx-ir-format.md` and the `nx-ir-format`, `typescript-ir-runtime`, `cli-code-generation` and `dotnet-binding` deltas for the prelude version, per-module resolution, the generated companions and the closed-formatter rule
- [x] 12.6 Settle how a range is recognized: state in `docs/nx-ir-format.md`, in an `nx-ir-format` requirement and at `integerRange` that a consumer recognizes a range by shape because a canonical value carries no module and a record's type arguments are erased, name where provenance is decided instead (the checker's prelude-origin test and nominal normalization through the site's slot), and state that an item's carrier is the backend's own numeric type; verify with `a_modules_own_range_does_not_iterate` and `the range the runtime iterates is the prelude's declaration` (RF14)
- [x] 12.7 Close the remaining test-coverage gaps: the `integerRange` refusal branches, a closed range at `MAX_SAFE_INTEGER`, the `count > limit` boundary and `maxRangeLength` through `evaluateComponent` in the TS runtime; a braced property value and a call argument in the TextMate tests, recording that an unbraced range property value is a syntax error rather than an untested case; JSON value kinds in `NxRange_TracksThePreludeDeclaration`; and a comment at the corpus resolver where the built-in prelude fallback is genuinely exercised (RF20)
- [x] 12.8 Declare each closed update formatter once per generated namespace rather than once per module: library output emits them into a shared `_NxFormatters.g.cs` under a name no module takes, carrying the dependency usings the naming modules bring, while single-file output keeps them beside the contracts; restate the `cli-code-generation` requirement and verify with `csharp_library_output_declares_a_closed_prelude_formatter_once` (RF22)
- [x] 12.9 Let one assembly hold two instantiations of a generic update companion: suppress `MsgPack009` in the generated C# header, which counts closed formatters against the open generic they share, and record in the `cli-code-generation` requirement why that is not the ambiguity the analyzer reads it as; verify with `two_instantiations_of_one_companion_each_get_a_closed_formatter` and by build — `Generated/prelude-companions.nx` gained a `float64` patch beside its `int` one, so `NxRangeCompanionTests` compiles the two closed formatters and round-trips both members (RF23)
- [x] 12.10 Narrow the "MessagePack cannot format this" warning to the one shape that still fails — a companion another assembly declares, reached below the member's own type, where `MsgPack006` comes from `CSC` and no file-level pragma reaches it — and let a companion the generating library declares generate with no member attribute at the member's type and below it, since the source generator closes the companion's own shim; restate the `cli-code-generation` requirement and its scenario, correct the stale `MsgPack009` reason in the emitter comment, and verify by build — `Generated/update-records.nx` gained `patch:<Range.Update T=int/>` and `many:<Range.Update T=int/>[]` on `Schedule`, which `AppliedTypeFields_RoundTripAsTheirInstantiations` round-trips — plus `a_generic_update_companion_csharp_cannot_format_is_reported` (RF24)
