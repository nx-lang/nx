## 1. Model: declaring module on companion fields

- [x] 1.1 Add `declaring_module: Option<String>` to `ExportedRecordField` in `crates/nx-cli/src/typegen/model.rs`, fill it in `export_update` from `EffectiveField.module_identity` (`None` when it equals the prepared module's identity), leave it `None` in every other constructor, and remove `ExportedUpdate.inherited_from_other_modules`. Verify `cargo build -p nx-cli` is clean and `update_companions_flatten_inherited_fields` still passes.
- [x] 1.2 Extend `CachedImportedLibrary` to keep what origin lookup needs (design D4): the library's `namespaces` and an index from `(module_identity, definition_id)` to the interface item's exported name, visibility, and kind; give `ImportedTypeCollector` an index from module identity to the cached library, and load transitive dependencies through `load_dependency` by walking `LibraryArtifact.dependency_roots`. Verify a model unit test that loads a library with a dependency of its own reports both libraries' module identities as resolvable.

## 2. Model: the resolution pass

- [x] 2.1 Add a pass over `Vec<ExportedModule>` (design D2) that, for every `ExportedType::Update` field with a declaring module, resolves each non-primitive type name in the declaring module's `ModuleNamespace`, maps the `DeclaringOrigin` to its owning library and interface item, and for a peer origin in library mode rewrites the name to the origin's exported name. Verify a model test with two library modules, where the base module imports `Tag` under an alias and the derived module is in the same library, shows the companion field typed by the exported name.
- [x] 2.2 For a dependency origin that is exported and cross-library-referenceable, choose the visible name (design D3), append an `ImportedType` to the generating module's `imported_types` when no existing entry points at the same library and exported name, and rewrite the field's `TypeRef` to that visible name. Verify a model test asserts the synthesized `ImportedType` (visible name `Tag`, exported name `Tag`, library `named`) and a second test where the generating module declares its own `Tag` gets visible name `named.Tag`.
- [x] 2.3 Run the pass from `from_artifact_with_warnings` and `from_library_with_warnings` before `build_from_exported_modules`, with the generating library's own namespaces and interface items indexed for peer resolution; delete `warn_about_unresolvable_inherited_fields` and emit the design D5 warning from the pass for a name that does not resolve, an origin that is not exported, or a kind with no cross-library representation. Verify `cargo test -p nx-cli typegen::model` passes and the warning text names the companion, the field, and the type.

## 3. Generation tests

- [x] 3.1 In `crates/nx-cli/src/typegen.rs`, add a temp-directory test with libraries `named` (exporting `Tag` and `Named`) and `people` (importing only `Named`, exporting `User extends Named`) that generates TypeScript and asserts `import type { Tag } from "<package>"` and `tag?: Tag` on `User_update`, and generates C# and asserts the `Tag` property is typed `global::<Namespace>.Tag`, with no "does not import" warning in either output. Verify the test passes.
- [x] 3.2 Add the alias scenario: library `tags` exports `Tag`, library `named` imports it as `t.Tag` (a selective import alias is a `Prefix.Name` qualifier) and uses `t.Tag` on `Named`, library `people` imports `Named`; assert the generated `people` output references the `tags` library's `Tag` and contains no declaration or import named `t.Tag` or `t_Tag`. Verify the test passes for both languages.
- [x] 3.3 Add the not-exported scenario: `named` declares `Tag` without `export`; assert the warning names `User_update`, `tag`, and `Tag`, and that the generated companion carries the bare name `Tag`. Verify the test passes.
- [x] 3.4 Confirm `library_update_companion_carries_fields_inherited_from_a_dependency_base` and `checked_in_dotnet_update_record_fixture_matches_typegen_output` still pass unchanged, and run `cargo test -p nx-cli`. Verify the whole crate suite is green.

## 4. Deferred test gaps from the follow-up doc

- [x] 4.1 In `crates/nx-api/src/artifacts.rs` tests, add a registry-backed test where a `TempDir` library exports `abstract type Named = { name:string }` and a source module imports it and constructs `<User.Update name="x" />` for `type User extends Named = { email:string }`; assert no error diagnostics, then assert a second module constructing `<User.Update nickname="x" />` reports an unknown-field error naming `nickname`. Verify both assertions pass.
- [x] 4.2 In `crates/nx-codegen/src/tests.rs`, add an execution test beside `generated_javascript_keeps_absent_update_fields_absent` with `component <Counter /> = { state { count:int = 0 label:string = "x" } <Label /> }` and `let root() = <Counter.Update count=1 />`, asserting the JSON output is exactly `{ "$type": "Counter.Update", "count": 1 }`. Verify the test passes locally with `node` present.
- [x] 4.3 In `bindings/dotnet/tests/NxLang.Sdk.Tests/NxUpdateRecordTests.cs`, add a test using the root-bound handler shape from design D6 (`let saveButton() = <Button onTapped=<User.Update name="Ada" /> />` rendered inside a stateless component), read the handler token from the initialized component's rendered JSON, dispatch it through `DispatchComponentActionsJson`, and assert the single effect's keys are exactly `$type` and `name` with `$type` equal to `User.Update`. Verify `dotnet test bindings/dotnet` passes.

## 5. Documentation

- [x] 5.1 In `docs/add-update-actions-followsup.md`, remove item 1, item 3, and the three bullets of item 5 this change lands, renumber the remaining items, and update the introductory paragraph. Verify the document reads coherently and the remaining items (property proposal, lazy `T.Update`, host resource limits, union case payloads) are unchanged.
- [x] 5.2 Run `cargo test --workspace`, `dotnet test` in `bindings/dotnet`, and `openspec validate resolve-inherited-companion-field-types --strict`. Verify all three are clean.
