## 1. Grammar, validation and the syntax tree

- [x] 1.1 In `crates/nx-syntax/grammar.js`, replace the `type` rule's `repeat(choice('?', seq('[', ']')))` with `optional(choice('?', '+', '*'))` plus a still-parsed `seq('[', ']')` alternative that validation rejects; add an optional `'?'` after the name in `property_definition`; remove `conditional_expression`; remove `null_literal`; add postfix `exists_expression` (`value_expression '?'`) and `optional_member_expression` (`value_expression '?.' identifier`) at member precedence, a right-associative `'??'` binary operator at precedence 125, and `seq('{', '}')` as a `pattern` alternative; regenerate with `tree-sitter generate` and verify the generator reports no new conflicts and `cargo build -p nx-syntax` succeeds
- [x] 1.2 In `crates/nx-syntax/src/validation.rs`, reduce `validate_type_suffix_chain` to "at most one occurrence suffix per chain, across parentheses, function results and aliases" with the diagnostic wording from `occurrence-types`, and add the `[]` rejection naming `*` and `+`; verify with `nx-syntax` tests that `string??`, `string?+`, `(string+)*`, `<function />: int*?` and `string[]` each report one error and `string?`, `string+`, `string*`, `(string)+` and `(<function />: string)+` are accepted
- [x] 1.3 Add a syntax error with the `if … { } else { }` fix-it for the `expr ? expr : expr` shape, and update `crates/nx-syntax/tests/parser_tests.rs`, its snapshots and `crates/nx-syntax/src/syntax_kind.rs` for the removed and added node kinds; verify `cargo test -p nx-syntax` passes and a fixture `let ratio = ready ? 1 : 2` reports the fix-it
- [x] 1.4 Rewrite the suffix, property-definition, pattern and expression prose in `nx-grammar.md` and `nx-grammar-spec.md` for the new grammar and remove the ternary; verify by reading each passage against `occurrence-types`, `optional-properties` and `presence-operators`

## 2. HIR

- [x] 2.1 In `crates/nx-hir/src/ast/types.rs`, replace `TypeRef::Array` and `TypeRef::Nullable` with `TypeRef::Seq { inner, occ }` where `occ` is the two-flag `Occurrence` from design.md, and update the source-spelling renderer to print `?`/`+`/`*` and parenthesize a function type under a suffix; verify `cargo test -p nx-hir` compiles and the renderer tests print the new spellings
- [x] 2.2 Add `optional: bool` to the property definition AST, remove `Literal::Null`, remove the `CONDITIONAL_EXPRESSION` lowering arm, and add `Expr::Exists`, `Expr::OptionalMember` and `Expr::Coalesce` with lowering from the new syntax kinds; lower a `{}` pattern to the existing empty `Expr::Array`; verify with `lower.rs` tests that `subtitle?:string` lowers with `optional`, `b.author?.name`, `x?` and `a ?? b` lower to the new nodes, and `test_lower_ternary_expression`/`test_lower_nested_ternary` are removed
- [x] 2.3 Update doc comments in `crates/nx-hir/src/ast/expr.rs` and `types.rs` so no comment offers `int[]`, `string?[]` or `null` as an example; verify by grep

## 3. The type model

- [x] 3.1 In `crates/nx-types/src/ty.rs`, replace `Type::Array` and `Type::Nullable` with `Type::Seq { item, occ }` and an `Occurrence` type providing `join`, `sum`, `product`, `without_zero`, `admits_zero`, `admits_many` and `Display`; make `Type::seq` refuse a `Seq` item; render `Person?`/`Person+`/`Person*` and the empty type as `{}`; verify with unit tests covering every lattice table row in `occurrence-types`
- [x] 3.2 Rewrite `common_supertype` in `crates/nx-types/src/semantics.rs` and the inference context's override to join item types and `Occurrence::join` occurrences, removing the nullability-lifting arm from the `implicit-primitive-conversions` join; make `type_satisfies_expected` use the lattice order with the one-level lift; verify with `crates/nx-types/tests/nullable_joins.rs` rewritten as `occurrence_joins.rs` covering `{}⊔T = T?`, `T⊔T+ = T+`, `T?⊔T+ = T*`, and `T?` rejected at `T`, `T*` at `T+`, `T+` at `T?`
- [x] 3.3 Make `item_contribution` and its callers in `infer.rs` — `Expr::Array`, content-property checking and `for` body inference — compute the collected occurrence by `Occurrence::sum` and the `for` result by `Occurrence::product`, leaving a single-item braced value as its item; verify with tests that `{1 2}` is `int+`, `{o 2}` is `int+`, `{o p}` is `int*`, `{o}` is `int?`, `for n in ns:int+ { n }` is `int+`, with body `if … { n }` is `int*`, and `for x in p:Person? { x.name }` is `string?`
- [x] 3.4 Type an `if`/`if … is` with no `else` as the join of its branches with the empty type, and make the match uncovered-path rule follow; verify `let v = { if c { 1 } }` infers `int?`, `if c { xs:string+ }` infers `string*`, and the exhaustiveness diagnostic is unchanged
- [x] 3.5 Enforce the property-slot rule at property type resolution: reject a resolved `?` or `*` type in any property definition's slot (including through an alias and for function-type parameters) with the `name?:T` fix-it, reject `?` on a `type` parameter, and reject a default on an optional property with both forms shown; verify with tests for `subtitle:string?`, `tags:string*`, `subtitle:Maybe`, `T?:type` and `subtitle?:string = "x"`
- [x] 3.6 Compute the read type of an optional property (`T?` for `p?:T`, `T*` for `p?:T+`) at member access, prop and state reads, and parameter reads; make record construction, component use, calls and host construction bind an omitted optional to the empty value and check a written value against the read type, and check a required or defaulted property against its declared type so `{}` is rejected there; verify with the `optional-properties` construction scenarios as tests in `crates/nx-types/tests/optional_properties.rs`
- [x] 3.7 Make a type argument require an exactly-one type in `applied_type` and `resolve_type_argument`, rejecting `<Box T=int?/>` and an alias to a `?` type with the "exactly one value" wording; update `record_type_parameters.rs` and `component_type_parameters.rs` tests and the `never*` spelling in the unspecified-parameter rule; verify `cargo test -p nx-types` passes
- [x] 3.8 Reverse the nullable-base rule at `infer.rs` ~2121 so `.` on a `?` receiver is rejected naming `?.`, `?.` on a `?` receiver types by the join table in `presence-operators`, `?.` on an exactly-one receiver reports unnecessary, and `.`/`?.` on a `+`/`*` receiver is rejected suggesting `for`; verify with tests for each row of the table and each diagnostic
- [x] 3.9 Type `Expr::Exists` as `boolean` with the always-present diagnostic, and `Expr::Coalesce` by `without_zero(left).join(right)` with the never-taken diagnostic; verify with tests for `n?` on `int`, `xs?` on `int*`, `n ?? 0`, `xs:int* ?? ys:int+` → `int+`, `o ?? p` → `int?`
- [x] 3.10 Add the path-keyed narrowing map to the inference environment with push/pop; decompose `if` conditions into `Exists(path)`, `Not(Exists(path))` and `And` and narrow the then/else branches accordingly, entering every receiver of a tested `?.` chain; consult the map in `Expr::Identifier`, `Expr::Member` and `Expr::OptionalMember`; verify with tests for each scenario under "A presence test narrows the tested path", including that `||` does not narrow and that a narrowing does not escape its branch
- [x] 3.11 Accept the `{}` pattern in `if … is` when the scrutinee admits zero, report it as never matching otherwise, count it toward exhaustiveness over a `?` union, and narrow a path scrutinee in the `else` arm and the arms after it; extend match narrowing from a local identifier to a path; verify with the three `{}`-pattern scenarios as tests
- [x] 3.12 Update `T.Update` derivation to mark each field clearable from the target's optional mark, accept `{}` or a zero-admitting value only for clearable fields, and derive `changed` as `T.Property*`; rewrite `crates/nx-types/tests/update_records.rs` and `update_intrinsics.rs`; verify `cargo test -p nx-types` passes
- [x] 3.13 Sweep `crates/nx-types` for the words "nullable" and "null" in diagnostics and doc comments and for `[]` in test fixtures, rewriting each; verify `grep -rn "nullable\|null" crates/nx-types/src` returns only the host-boundary decoder and `cargo test -p nx-types` passes

## 4. Values and the interpreter

- [x] 4.1 Remove `NxValue::Null` from `crates/nx-value/src/lib.rs` and `Value::Null`/`SerializedValue::Null` from `crates/nx-interpreter`, making the empty array the empty value; decode host `null`, `[]` and a missing key to it at a `?`/`*` site, decode a one-element array at a `?` site to its element, reject `null`/`[]` at a `+` or exactly-one site, and encode a present empty update-record field as `null`; verify with `nx-value` round-trip tests for each row of "Canonical encoding of an occurrence-typed value"
- [x] 4.2 Make record construction store no entry for an empty optional field, make `eval_member` yield the empty value for an unstored declared-optional field, and make update-record construction store a present empty field; verify with interpreter tests that `<Book title="A" />` has no `author` key in canonical JSON, `b.author` evaluates to `{}`, and `<User.Update email={} />` encodes `email: null`
- [x] 4.3 Rewrite `coerce_value_to_resolved_type` for `Type::Seq`: un-lift `[x]` at a `?` site, lift `x` at `+`/`*`, leave `[]`, and error on `[]` at `+`/exactly-one with the field named; verify with `crates/nx-interpreter/tests/arrays.rs` and the "An empty array is rejected where at least one is required" scenario
- [x] 4.4 Evaluate `Expr::Exists`, `Expr::OptionalMember` and `Expr::Coalesce` on the empty-array representation, evaluating `??`'s right operand only when the left is empty and the receiver of `?.` once; match the `{}` pattern against the empty value; verify with `crates/nx-interpreter/tests/conditionals.rs` cases for each `presence-operators` runtime scenario, including the `fail()` short-circuit
- [x] 4.5 Update the four update intrinsics for absent-versus-empty and `changed` yielding a sequence; delete every `Value::Null` test expectation and the `null =>` pattern fixtures; verify `cargo test -p nx-interpreter` passes with no reference to `Null`

## 5. IR, the TypeScript IR runtime and generated JavaScript

- [x] 5.1 In `crates/nx-codegen/src/ir.rs`, bump `NX_IR_SCHEMA_VERSION` to 5, add the `seq` type kind with an occurrence field and the `exists`, `optionalMember` and `coalesce` node kinds with new numbers, add `NX_IR_REQUIRED_FEATURE_OCCURRENCE_V1`, and stop emitting `array`, `nullable` and `null` while leaving their numbers assigned with a comment; update `crates/nx-codegen/src/builder.rs` to emit `seq` for every `Type::Seq` (including erased type parameters as `object` under the same occurrence) and to declare the feature when an operator node is emitted; verify with builder tests and `cargo test -p nx-codegen`
- [x] 5.2 In `runtime/typescript/src/index.ts`, accept schema 5, evaluate the three node kinds and the `{}` pattern, make `normalizeValue` un-lift at `?` and reject empty at `+`, decode `null`/`[]`/missing to the empty value and drop empty optional record fields, normalize update patches with the clearable rule, bind content by declared occurrence (one lone-child rule), and remove `truthy` and every `null` path; verify `npm test` in `runtime/typescript` passes with `runtime.test.ts` cases for each
- [x] 5.3 In `crates/nx-codegen/src/emit.rs`, emit the three operators over the empty-array representation, emit `int+`/`int*` as `readonly number[]` and `T?` as `T | NxEmpty` (the empty array) in type positions, drop every `null` emission, and emit an omitted key for an empty optional field; verify with emission tests and `cargo test -p nx-codegen`
- [x] 5.4 Add conformance corpus cases under `specs/ir-conformance/` for `x?`, `x?.m`, `x ?? y`, the `{}` pattern, an optional field omitted and written empty, a `+` field fed one child, and an update record clearing a field; rewrite every existing case that spells `[]` or `null`; verify `corpus.test.mjs` and `emitted-ir.test.mjs` pass with the interpreter, the IR runtime and generated JavaScript agreeing value-for-value, including the lone-child case that today excludes the IR runtime

## 6. Typegen, FFI and SDKs

- [x] 6.1 In `crates/nx-cli/src/typegen/languages/csharp.rs` and `typescript.rs`, map `T?` to nullable/`T | null`, `p?:T` to a nullable property/`p?: T`, `T+`/`T*` to the existing list mappings, drop `null` from supported literal defaults, mark update companion fields clearable or not (`email?: string | null` versus `name?: string`), and emit typed property keys with nullable value types for optional fields; rewrite the typegen tests in `crates/nx-cli/src/typegen.rs` and `main.rs`; verify `cargo test -p nx-cli` passes and the generated C# for a fixture with all four property shapes compiles under `dotnet build`
- [x] 6.2 In `crates/nx-ffi` and `bindings/dotnet`, replace "set to null" with "cleared" in the managed update-record API while keeping `= null` as its C# spelling, reject clearing a non-clearable field at construction, decode `null`/`[]`/missing as empty, and validate `+` non-emptiness at the boundary; verify the .NET binding tests pass and a managed `new Book_update { Author = null }` for `author?:Person` round-trips as `email: null`
- [x] 6.3 In `bindings/node` and `bindings/wasm`, update the canonical value decoders/encoders for the empty value and remove `null` variants; verify each package's tests pass and the wasm SDK compiles the fixture from 6.1

## 7. Formatter, editor grammar and language service

- [x] 7.1 In `crates/nx-cli/src/format.rs` (the NX-syntax *value* printer; the repository has no source formatter, so the suffix, `name?:`, operator and pattern forms have nothing to print them), print the empty value as `{}`; verify the round-trip tests pass over a fixture containing every occurrence form and `grep -c '\*/>'` over printed output is zero
- [x] 7.2 In `src/vscode/syntaxes/nx.tmLanguage.json`, scope `+` and `*` with `?` as `keyword.operator.type-modifier.nx`, add the property-name `?` scope and the three operator scopes from the `editor-syntax-highlighting` delta, remove the ternary and `constant.language.null.nx` rules, and drop `[]`; update the highlighting tests and the `src/vscode/samples` fixtures; verify the token-stream tests pass and a whole-repository token diff shows only the intended scope changes
- [x] 7.3 In `crates/nx-language-service`, render hover types by `Type::Display` (no "nullable"), offer no `null` completion, and keep `BUILTIN_TYPE_COMPLETIONS` unchanged; verify the hover tests in `hover.rs` print `Person?`, `Person+`, `Person*` and `{}`

## 8. Corpus, examples and documentation

- [x] 8.1 Build a baseline `nxlang` from `main` and record canonical output and diagnostics for every `.nx` file in `examples/`, `sites/playground/src/examples/nx/`, `specs/ir-conformance/`, `src/vscode/samples/` and `docs/`; verify the baseline is stored under the scratchpad for the diff in 8.5
- [x] 8.2 Rewrite the 45 `.nx` files spelling `[]` (`T+` unless the text says the sequence may be empty), the 11 writing `null` (`{}`, omission, or `{} =>`), and the 3 using a ternary (`if/else`), including `examples/nx/types.nx`'s `null =>` arm and `examples/nx/generic-records.nx`'s `T?` argument; verify every file parses and type-checks with the new `nxlang`
- [x] 8.3 Rewrite `docs/src/content/docs/reference/syntax/types.md`, `reference/syntax/expressions.md`, `reference/syntax/if.md`, `reference/concepts/sequences-and-objects.md`, `language-tour/types.md` and `language-tour/expressions.md` for occurrences, optional properties, the three operators and the `{}` pattern, noting that `??` binds above arithmetic unlike C#; verify every NX snippet in those pages parses with the new `nxlang`
- [x] 8.4 Update `sites/playground/scripts/generate-catalog.mjs` and the playground's DrawnUI catalog so an optional TS array prop becomes `name?:T+` and an optional prop `name?:T`, and regenerate; verify `sites/playground/scripts/check-examples.mjs` passes
- [x] 8.5 Run the whole-corpus diff against the 8.1 baseline; verify every file that parsed still parses, canonical output is identical except that an absent optional field is an omitted key rather than `null`, and the only new diagnostics are the intended `+`-site rejections, each reviewed and either fixed in the source or accepted with a note in the change's review notes

## 9. Specs and future notes

- [x] 9.1 Delete `openspec/specs/type-reference-suffixes/` at archive time (its delta removes every requirement) and confirm `openspec validate --strict` accepts the change; verify the validate output
- [x] 9.2 Sweep the main specs not covered by a delta for `[]`, `null` and `x:T?` spellings in prose (not requirements) and fix them directly; verify `grep -rn "\[\]\|\bnull\b" openspec/specs` returns only host-language `null` in `sdk-node`, `workspace-programs` and `dotnet-binding`'s workspace-argument rule, and the update-record wire form
- [x] 9.3 Replace the "Removing `null`: `?` as an occurrence indicator" section of `specs/future.md` with a short pointer to this change and a list of what it deliberately left open (mapping `?.` over sequences, `T*` in a property slot, hover rendering of `{}`, the `object?` ambiguity, `never` as a writable name); verify the section reads as a record rather than a plan
- [x] 9.4 Draft the `v0.4.0` GitHub release notes (per `docs/deployment.md`) with the breaking changes and the migration table from `type-reference-suffixes`' REMOVED entries; verify the draft lists `[]` → `+`/`*`, `null` → `{}`, `x:T?` → `x?:T`, and the ternary removal

## 10. Function parameters (review finding RF29)

- [x] 10.1 Reject a required parameter after an omissible one in a paren-style signature (`validate_paren_parameter_order`), record each function's form and parameter defaults in HIR and library interfaces, and in the checker accept a positional call that stops before trailing omissible parameters, reject a positional call of an element-style function, relax element calls for defaulted parameters, and check each default against its parameter with only the parameters before it in scope; verify `crates/nx-types/tests/function_parameters.rs`
- [x] 10.2 Fill a parameter a call leaves out inside the callee in the interpreter (positional, element, function-value and host calls), running a function declared in another module in that module's context, and in the IR (a default node per parameter, an absent call argument), the TypeScript IR runtime and generated JavaScript (JavaScript default parameters); verify `crates/nx-interpreter/tests/function_parameters.rs`, the `runtime.test.ts` default case, and the `parameter-defaults` corpus program agreeing in all three engines
