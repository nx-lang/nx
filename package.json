{
  "name": "nx-workspace",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "verify:packages": "pnpm -r --filter \"./packages/*\" run verify:package"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
