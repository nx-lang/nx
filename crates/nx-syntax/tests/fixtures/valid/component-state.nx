import "./ui" as Ui

component <SearchBox placeholder:string /> = {
  state {
    query:string
  }

  if isReady {
    <Ui.TextInput value={query} placeholder={placeholder} />
  } else {
    <Spinner />
  }
}

<SearchBox placeholder="Find docs" />
