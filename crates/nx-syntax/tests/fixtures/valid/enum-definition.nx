enum Status = pending_review | active | disabled

enum Direction = | north | south | east | west

let <Indicator current:Direction /> = {
    if current == Direction.north {
        1
    } else {
        0
    }
}
