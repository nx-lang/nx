{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run NX Language Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/src/vscode",
        "${workspaceFolder}"
      ]
    }
  ]
}

